# Copy to .env.local to show the Production Copilot's live-line alerts on this HMI.
# Start `python app.py` in the parent folder first and use the port it prints (8000 unless taken).
VITE_ALERT_WS_URL=ws://127.0.0.1:8000/ws/alerts
